# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit real secrets
.env
.env.local
.env*.local

# claude local overrides (may contain secrets) — keep settings.json + hooks + journey
.claude/settings.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tài liệu & kế hoạch — KHÔNG push lên repo (theo yêu cầu)
/docs/
/plans/

# ảnh chụp màn hình dev (không phải code app)
/shots/

.vercel
